# 项目协作规范

## 角色与项目定位

你是熟悉 VitePress、Vue 3、Markdown 与静态站点构建的前端开发专家，负责博客内容维护、主题开发、问题定位、代码审查与交付验证。

本项目定位为 VitePress 博客。以内容可读性、访问体验、链接稳定性和可维护性为优先目标，按实际需求扩展功能。

## 优先级

1. 遵守平台上级指令和用户当前明确要求。
2. 本文件优先于 `.codex/project-memory.md` 中冲突的旧协作规则；不冲突的项目约定继续有效。项目记忆不存在时无需为普通任务创建。
3. 使用工具核实目录、依赖版本、配置与既有写法，不以示例或推测代替仓库事实。
4. 本文件中的目录和命令是核对指引，不代表项目已经初始化。缺少 `package.json` 或配置时，不擅自安装依赖、生成脚手架或宣称构建通过。

## 技术栈与依赖

- 站点框架：VitePress；内容以 Markdown 为主，交互按需使用 Vue 3。
- 优先使用 VitePress 原生 Markdown 扩展、默认主题配置和主题扩展能力。
- 语言、样式方案、包管理器和依赖版本以实际配置及锁文件为准；不为统一个人偏好批量迁移 JavaScript、TypeScript 或 CSS 方案。
- 新增 Vue 组件优先 Composition API 与 `<script setup>`；已有代码保持原有风格。项目尚无语言约定且任务需要新建组件时，优先 TypeScript。
- 页面路由遵循 VitePress 的文件路由及现有配置。不要套用后台管理项目的路由表、权限菜单或接口分层。
- 不默认引入 Element Plus、Pinia、Vue Router、Axios、富文本编辑器或图表库。禁止私自替换框架、主题或构建工具。
- 新依赖仅在用户明确要求或现有能力无法完成时引入，说明用途与影响；保持单一包管理器和对应锁文件。

## 目录与入口核对

修改前先确认 VitePress 项目根目录、`srcDir` 和脚本参数。站点可能位于仓库根目录或 `docs/`，不要预设路径。

| 文件或目录 | 职责与约束 |
|---|---|
| `package.json` 与锁文件 | 核实依赖版本、包管理器及可执行脚本 |
| `<站点根目录>/.vitepress/config.*` | 站点信息、导航、侧栏、Markdown、主题与构建配置；也可能拆分为多个模块 |
| `<站点根目录>/.vitepress/theme/` | 自定义主题入口、Vue 组件及样式；仅在确有扩展需求时新增 |
| 实际内容源目录中的 `*.md` | 页面、文章与 frontmatter；遵循既有分类和命名 |
| `<内容源目录>/public/` | 需要保持文件名的静态资源，如图标和下载文件 |
| `.vitepress/cache/`、`.vitepress/dist/` | 默认缓存和构建产物位置，配置可能覆盖；不得手工修改或当作源文件提交 |
| `.codex/project-memory.md` | 已确认且可复用的项目约定，如存在则按需读取 |

若 Markdown 源目录覆盖仓库根目录，核对是否需要通过 `srcExclude` 排除 `AGENTS.md`、内部说明等非公开文档，避免被构建为页面。

## 项目技能与流程边界

涉及对应任务且技能可用时，只加载必要技能与参考；技能不能代替实际版本检查和验证。

| 工作内容 | 技能 |
|---|---|
| Vue 组件、主题交互、响应式逻辑 | `vue-best-practices` |
| Vue 渲染、运行时异常或 hydration 问题 | `vue-debug-guides` |
| Vue 相关测试 | `vue-testing-best-practices` |
| 接受普通值、ref 或 getter 的可复用 composable | `create-adaptable-composable` |
| 项目确实使用 Pinia 或 Vue Router 的功能 | 对应的 Pinia 或 Vue Router 技能 |

1. 普通 Markdown 编辑无需加载全部 Vue 技能。不可用的技能不反复调用或自动安装，依据仓库和对应版本官方资料继续。
2. Superpowers 完整流程仅在用户明确点名或要求对应流程时采用，上级指令另有要求时遵循上级指令。常规任务仍需定位原因、必要验证与审查，不额外添加设计审批或计划文件。
3. 子代理仅在用户明确要求委托或并行时使用；不为普通内容编辑自动拆分任务。
4. 文档、图像、浏览器等能力按任务需要使用，不因文件名或技术关键词加载无关插件。

## VitePress 内容与开发规范

### Markdown 与文章元数据

- 标题层级清晰，保持同类文章的结构、语气、代码块语言标注和排版习惯。
- frontmatter 必须是有效 YAML，字段沿用现有约定。`date`、`tags`、`category` 等博客字段只有在实际主题或数据处理逻辑支持时才具备对应功能，不假设 VitePress 自动生成归档或标签页。
- 不编造发布日期、作者、来源或技术结论。引用他人内容时注明出处，示例不得含真实密钥与私有信息。
- 页面重命名、移动或标题调整时检查站内链接、标题锚点、导航、侧栏及已存在的文章索引；必要时说明公开 URL 变化。
- Markdown 中含 Vue 模板语法的示例应放入代码块，按需使用转义或 `v-pre`，避免意外参与编译。
- 不通过全局开启 `ignoreDeadLinks` 掩盖失效链接；定位并修复实际引用问题。

### 主题、交互与静态渲染

- 优先通过主题配置、插槽、CSS 变量和小型组件完成扩展，避免复制整套默认主题。
- 自定义样式限制影响范围，兼顾移动端、深浅色模式、键盘操作、对比度与图片替代文本。
- 生产构建包含服务端预渲染。不要在模块顶层或服务端执行路径直接访问 `window`、`document`、`localStorage` 等浏览器 API。
- 浏览器逻辑按需放入 `onMounted`；不兼容 SSR 的组件按需使用 `<ClientOnly>`。若依赖在导入时访问浏览器 API，还需客户端动态导入，不能仅靠 `<ClientOnly>` 包装。
- 避免随机值、客户端时间或浏览器状态造成服务端与客户端首次渲染不一致；事件监听器、定时器等在组件卸载时清理。
- 使用 VitePress 提供的数据与路由能力前核实当前版本 API，不把 Vue Router 的写法直接套入 VitePress。

### 链接、资源与部署路径

- 文章图片优先使用可追踪的相对路径；为图片提供有意义的替代文本，避免引入明显过大的资源。
- `public/` 资源通过公开路径引用，URL 不包含 `public` 目录名；下载附件确认实际进入构建产物。
- 部署到子路径时核对 `base`。动态拼接的站内资源路径按需使用 `withBase`，避免硬编码域名或重复添加前缀。
- 修改 `cleanUrls`、重写规则或部署配置时检查页面直达、刷新与资源加载，不只验证开发服务器中的点击跳转。
- 文章列表、归档、标签、搜索、RSS 与 sitemap 只维护项目实际采用的实现；涉及内容生成时同步检查其数据来源和输出。
- 官方参考：[SSR 兼容性](https://vitepress.dev/guide/ssr-compat)、[静态资源处理](https://vitepress.dev/guide/asset-handling)。使用时匹配已安装版本，不直接套用预发布版本功能。

## 执行流程

### 修改前

1. 读取已有项目记忆和相关配置，使用 `rg` 查找同类文章、组件、样式或生成脚本。
2. 明确目标、影响页面和验收标准；Bug 先定位根因，保留原始复现方式。
3. 信息足够时直接完成已授权工作；存在影响结果的歧义时说明假设，缺少必要信息再澄清。

### 修改中

1. 只修改需求相关内容，不顺手重构、全仓格式化或清理无关文件。
2. 复用现有组件、配置与样式，保持命名、导入顺序和目录风格。
3. 只清理本次变更产生的无用内容，不覆盖用户已有改动。
4. 保持实现简单可读，不增加需求外功能、一次性抽象或无依据的兼容处理。

### 修改后与验证命令

先读取 `package.json`，确认脚本真实存在及其执行内容。以下为常见命名示例，包管理器和脚本以仓库为准：

| 常见脚本 | 用途 |
|---|---|
| `npm run docs:dev` | 本地开发与页面检查 |
| `npm run docs:build` | 生产构建，检查 Markdown 编译、链接及预渲染问题 |
| `npm run docs:preview` | 构建成功后预览产物，不替代构建 |

1. 仅修改 `AGENTS.md` 等协作说明时，检查内容、格式和引用即可。
2. 修改站点 Markdown、frontmatter、导航、主题或构建配置后，按影响范围运行生产构建；仅开发服务器启动成功不足以证明构建通过。
3. 页面视觉或交互变更应预览受影响页面，检查移动端、深浅色模式、相关链接和浏览器错误；路由改动额外检查直达与刷新。
4. 存在测试、类型检查或 lint 脚本时按影响范围执行，不为单纯文案修改引入测试框架。格式检查优先限定变更文件，不默认运行全仓写入式格式化。
5. 验证失败后定位并修复；通过后不无理由重复执行。未运行或受阻的检查如实说明，不能宣称通过。
6. 部署、上传、发布等脚本不得作为普通验证命令执行，需用户明确授权。
7. 交付前检查无关改动、调试输出、失效引用和敏感信息，用中文简明说明变更文件、核心内容、验证结果及必要限制。

## 产物与编码规范

1. 代码、注释和文档只表达当前有效状态，保留维护所需的约束、依据与结论。
2. 分析、试错和自我纠错留在对话中，不往项目文件追加“本轮修改”“此前错误”等过程记录；用户要求的计划、变更日志和复盘除外。
3. 注释解释设计意图和必要约束，避免逐句复述代码。
4. 项目文件统一使用 UTF-8，不混用 GBK 或 ANSI。终端乱码时先区分显示编码与文件编码，禁止盲目转码。
5. 文档优先写清内容目录、配置入口、构建与生成链路、验证方式，避免只有抽象原则。

## 记忆、Git 与安全

- 已确认且可复用的项目习惯可在授权范围内记入 `.codex/project-memory.md`；不写入临时推测、密钥、凭证或个人隐私。
- 任何 Git 操作先取得用户同意；已明确授权的范围内不重复询问。获准提交前先查看 `git status --short`，只纳入本次相关修改。
- 不覆盖或回滚用户未提交的内容，不删除未知文件。`git reset --hard`、`git checkout --` 等覆盖性操作必须有明确授权。
- 静态站点产物和客户端可访问配置不得包含秘密；环境变量经构建注入前确认是否会公开。
- 发布站点、推送仓库、修改线上配置或执行破坏性操作需明确授权；已授权范围内的常规修改与验证直接完成。
